import React from "react";

/**
 * NotAuthorizedPage Component
 *
 * This component is a simple component that is displayed when a user
 * attempts to access a page or a feature they are not authorized to access.
 *
 * @component
 *
 * @example
 *
 * return (
 *   <NotAuthorizedPage />
 * );
 */
const NotAuthorizedPage = () => {
  return (
    <div>NotAuthorizedPage</div> // Text message indicating the user isn't authorized
  );
};
export default NotAuthorizedPage;
